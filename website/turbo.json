{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "inputs": [
        "$TURBO_DEFAULT$"
      ],
      "outputs": [
        "build/**"
      ]
    },
    "test": {
      "outputs": []
    }
  }
}
